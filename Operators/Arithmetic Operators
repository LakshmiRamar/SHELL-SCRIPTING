!#/bin/bash

a=20
b=10

add=$((a + b))
echo Addition of a and b are $add

sub=$((a - b))
echo Subraction of a and b are $sub

mul=$((a * b))
echo Multiplication of and b are $mul

div=$((a / b))
echo Division of a and b are $div

mod=$((a % b))

echo Modulus of a and b are $mod
